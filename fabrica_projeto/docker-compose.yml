version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: polymarketing-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: polymarketing_db
      POSTGRES_USER: polymarketing_user
      POSTGRES_PASSWORD: polymarketing_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U polymarketing_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: polymarketing-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data: